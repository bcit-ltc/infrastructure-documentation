# Runs `.github/workflows/build-and-push-app.yaml`
name: Build and push app/chart images

on:
  push:
    branches: [ main ]

jobs:
  build-and-ship:
    uses: bcit-ltc/.github/.github/workflows/build-and-push-app.yaml@main
    permissions:
      contents: write
      packages: write
      id-token: write
      attestations: write
      pull-requests: write
      issues: write
    secrets: inherit

  upload-cdn-assets:
    needs: build-and-ship
    runs-on: ubuntu-latest
    env:
      APP_VERSION: ${{ needs.build-and-ship.outputs.app_version }}
      IMAGE: ${{ needs.build-and-ship.outputs.app_image }}

      # App-specific static path inside container:
      # for infrastructure-documentation this is correct:
      STATIC_ROOT: /usr/share/nginx/html
      STATIC_SUBDIR: assets

      # CDN config (shared via org/repo vars + secret)
      CDN_ACCOUNT_NAME: ${{ vars.COMMONCDN_ACCOUNT_NAME }}     # e.g. "commoncdnabcd1234"
      CDN_CONTAINER: ${{ vars.COMMONCDN_CONTAINER_NAME }}      # e.g. "cdn"
      CDN_NAMESPACE: ${{ github.repository_owner }}            # "bcit-ltc"
      CDN_APP: ${{ github.event.repository.name }}             # "infrastructure-documentation"
    steps:
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull built image
        run: |
          set -euo pipefail
          docker pull "${IMAGE}:${APP_VERSION}"

      - name: Extract static files from image
        run: |
          set -euo pipefail
          CID="$(docker create "${IMAGE}:${APP_VERSION}")"
          mkdir -p site
          docker cp "${CID}:${STATIC_ROOT}/." site
          docker rm "${CID}"
          ls -R site

      - name: Upload assets to CDN
        uses: azure/CLI@v2
        env:
          SAS_TOKEN: ${{ secrets.COMMONCDN_SAS_TOKEN }}
        with:
          inlineScript: |
            set -euo pipefail

            # e.g. bcit-ltc/infrastructure-documentation/1.1.12
            PREFIX="${CDN_NAMESPACE}/${CDN_APP}/${APP_VERSION}"

            echo "Uploading site/${STATIC_SUBDIR} -> ${PREFIX}/${STATIC_SUBDIR} in container ${CDN_CONTAINER}"

            az storage blob upload-batch \
              --account-name "${CDN_ACCOUNT_NAME}" \
              --sas-token "${SAS_TOKEN}" \
              --destination "${CDN_CONTAINER}" \
              --destination-path "${PREFIX}/${STATIC_SUBDIR}" \
              --source "site/${STATIC_SUBDIR}" \
              --overwrite true

