apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: documentation-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: documentation
                port:
                  number: 80
