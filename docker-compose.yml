name: infrastructure-documentation
services:
  app:
    image: infrastructure-documentation
    build:
      target: build
    environment:
      - DEBUG=true
      - CI=false
    volumes:
    - .:/app
    ports:
      - "8000:8000"
