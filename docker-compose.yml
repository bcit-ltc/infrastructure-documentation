name: infrastructure-documentation
services:
  app:
    image: infrastructure-documentation
    build:
      target: build
    environment:
      - DEBUG=true
      - CI=false
    volumes:
    - .:/app
    ports:
      - "8080:8000"
